<UserControl x:Class="WPFClientExample.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFClientExample.Views"        
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"     
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}"  CommandParameter="{Binding ElementName=pb_LoginPassword}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Login Page" FontSize="24" HorizontalAlignment="Center"/>
            <TextBox Width="200" Height="30" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
            <PasswordBox x:Name="pb_LoginPassword" Width="200" Height="30" VerticalContentAlignment="Center"/>
            <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=pb_LoginPassword}" Width="100" Height="30" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
